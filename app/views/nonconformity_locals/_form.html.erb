<%= form_with(model: nonconformity_local) do |form| %>
  <% if nonconformity_local.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(nonconformity_local.errors.count, "error") %> prohibited this nonconformity_local from being saved:</h2>

      <ul>
        <% nonconformity_local.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :local, style: "display: block" %>
    <%= form.text_field :local %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
